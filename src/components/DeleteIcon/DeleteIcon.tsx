import React from 'react';
import Feather from 'react-native-vector-icons/Feather';

const DeleteIcon = () => {
  return <Feather name="delete" size={30} color="#000" />;
};

export default DeleteIcon;
